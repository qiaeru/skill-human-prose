---
type: llm
weight: 1
---

The response returns the whole file as JSON, with the same keys in the same order; only string values differ from the input. Commentary outside the file doesn't count against this criterion.
